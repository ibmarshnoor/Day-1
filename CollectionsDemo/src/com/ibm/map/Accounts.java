package com.ibm.map;

import java.util.ArrayList;
import java.util.List;

import com.ibm.set.Bank;

public class Accounts {
      public static void main(String[] args) {
    	  Bank[] Account=new Bank[10];
    	  
    	  List<Bank> accounts=new ArrayList<Bank>();
    	/*   Bank accountName=new Bank();
    	   Bank accountBalance=new Bank();*/
    	   
    	   for (int i = 0; i < Account.length; i++) {
		/*	accountName.setName("Ram");
			accounts.add(accountName);
			accountBalance.setBalance(100);
			accounts.add(accountBalance);*/
		}
    	//  accounts.add(accountName);
    	 // accounts.add(accountBalance);
    	 System.out.println(accounts);
      }
}
